name: Victoria Wednesday
on:
  schedule:
    - cron: '25 00 * * WED'
    - cron: '50 00 * * WED'
    - cron: '00 02 * * WED'
    - cron: '10 02 * * WED'
    - cron: '15 03 * * WED'
    - cron: '45 03 * * WED'
    - cron: '20 07 * * WED'
    - cron: '30 08 * * WED'
    - cron: '30 13 * * WED'
    - cron: '50 14 * * WED'
    - cron: '50 15 * * WED'
    - cron: '25 16 * * WED'
    - cron: '00 19 * * WED'

jobs:
 run-script:
      runs-on: ubuntu-latest

      steps:
        - name: Checkout repository
          uses: actions/checkout@v2

        - name: Set up Python
          uses: actions/setup-python@v2
          with:
            python-version: '3.x'

        - name: Install dependencies
          run: |
            if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

        - name: Run Python script
          run: python main.py
    
