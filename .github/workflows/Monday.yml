name: Dynamic Schedule Python Script
on:
  schedule:
    - cron: '05 00 * * MON'
    - cron: '27 00 * * MON'
    - cron: '37 01 * * MON'
    - cron: '47 01 * * MON'
    - cron: '52 02 * * MON'
    - cron: '22 03 * * MON'
    - cron: '57 06 * * MON'
    - cron: '07 08 * * MON'
    - cron: '07 13 * * MON'
    - cron: '27 14 * * MON'
    - cron: '27 15 * * MON'
    - cron: '02 16 * * MON'
    - cron: '31 06 * * MON'

jobs:
 run-script:
      runs-on: ubuntu-latest

      steps:
        - name: Checkout repository
          uses: actions/checkout@v2

        - name: Set up Python
          uses: actions/setup-python@v2
          with:
            python-version: '3.x'

        - name: Install dependencies
          run: |
            if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

        - name: Run Python script
          run: python main.py
    
